---
import ToolLayout from '../../components/ToolLayout.astro';
---
<ToolLayout title="Flooring Cost Calculator" description="Compare flooring material and installation costs for hardwood, tile, laminate, vinyl, and carpet." icon="🪵" category="Cost Estimators" categorySlug="cost-estimators">
  <div slot="calculator">
    <div class="grid md:grid-cols-2 gap-6">
      <div><label class="block text-sm font-semibold text-[#1B4965] mb-1">Room Area (sq ft)</label><input type="number" id="area" value="200" min="1" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35] focus:border-transparent" /></div>
      <div><label class="block text-sm font-semibold text-[#1B4965] mb-1">Flooring Type</label>
        <select id="mat" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35]">
          <option value="3,8,2,4">Laminate ($3-8 material, $2-4 install)</option>
          <option value="2,7,2,4">Luxury Vinyl Plank ($2-7, $2-4 install)</option>
          <option value="5,12,4,8" selected>Hardwood ($5-12, $4-8 install)</option>
          <option value="8,15,6,10">Engineered Hardwood ($8-15, $6-10 install)</option>
          <option value="1,5,3,7">Ceramic Tile ($1-5, $3-7 install)</option>
          <option value="5,15,5,10">Porcelain Tile ($5-15, $5-10 install)</option>
          <option value="1,5,1,2">Carpet ($1-5, $1-2 install)</option>
          <option value="8,20,8,14">Natural Stone ($8-20, $8-14 install)</option>
        </select>
      </div>
      <div><label class="block text-sm font-semibold text-[#1B4965] mb-1">Installation</label>
        <select id="install" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35]">
          <option value="pro" selected>Professional installation</option>
          <option value="diy">DIY (material only)</option>
        </select>
      </div>
      <div><label class="block text-sm font-semibold text-[#1B4965] mb-1">Remove Old Flooring?</label>
        <select id="remove" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35]">
          <option value="0">No</option>
          <option value="1.50" selected>Yes ($1-2/sqft)</option>
        </select>
      </div>
    </div>
    <button onclick="calc()" class="mt-6 bg-[#FF6B35] hover:bg-[#e55a2b] text-white font-semibold px-8 py-3 rounded-lg transition-colors w-full md:w-auto">Estimate Flooring Cost</button>
    <div id="result" class="mt-6 hidden"><div class="bg-[#1B4965]/5 rounded-xl p-6"><h3 class="font-bold text-[#1B4965] text-lg mb-3">Flooring Cost Estimate</h3><div id="res"></div></div></div>
  </div>
  <Fragment slot="content">
    <h2>Flooring Costs Compared</h2>
    <p>Flooring costs vary widely. Budget-friendly laminate or vinyl runs <strong>$5-12 per sq ft installed</strong>, while hardwood and natural stone can reach <strong>$15-35 per sq ft installed</strong>.</p>
    <h2>Best Flooring by Room</h2>
    <ul>
      <li><strong>Kitchen/Bathroom</strong>: Tile, LVP, or vinyl — moisture resistant</li>
      <li><strong>Living/Bedroom</strong>: Hardwood, engineered wood, or carpet</li>
      <li><strong>Basement</strong>: LVP or tile — handles moisture</li>
      <li><strong>High traffic</strong>: Porcelain tile or LVP — most durable</li>
    </ul>
  </Fragment>
</ToolLayout>
<script is:inline>
function calc(){
  const area=parseFloat(document.getElementById('area').value);const areaPlus10=area*1.1;
  const[matLo,matHi,instLo,instHi]=document.getElementById('mat').value.split(',').map(Number);
  const pro=document.getElementById('install').value==='pro';
  const remove=parseFloat(document.getElementById('remove').value)*area;
  const costLo=Math.round(areaPlus10*matLo+(pro?areaPlus10*instLo:0)+remove);
  const costHi=Math.round(areaPlus10*matHi+(pro?areaPlus10*instHi:0)+remove);
  document.getElementById('result').classList.remove('hidden');
  document.getElementById('res').innerHTML=`
    <div class="text-center mb-4"><div class="text-4xl font-bold text-[#FF6B35]">$${costLo.toLocaleString()} – $${costHi.toLocaleString()}</div><div class="text-sm text-gray-500">${pro?'Installed':'Materials only'} (incl. 10% waste)</div></div>
    <div class="grid sm:grid-cols-3 gap-4">
      <div class="text-center"><div class="text-lg font-bold text-[#1B4965]">$${matLo}-$${matHi}/ft²</div><div class="text-xs text-gray-500">Material</div></div>
      <div class="text-center"><div class="text-lg font-bold text-[#1B4965]">${pro?'$'+instLo+'-$'+instHi+'/ft²':'$0'}</div><div class="text-xs text-gray-500">Installation</div></div>
      <div class="text-center"><div class="text-lg font-bold text-[#1B4965]">$${Math.round(remove)}</div><div class="text-xs text-gray-500">Removal</div></div>
    </div>`;
}
</script>
