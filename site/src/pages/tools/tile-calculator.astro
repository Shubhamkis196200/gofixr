---
import ToolLayout from '../../components/ToolLayout.astro';
---
<ToolLayout title="Tile Calculator" description="Calculate how many tiles you need for floors, walls, or backsplashes — with waste factor." icon="🔲" category="Material Calculators" categorySlug="material-calculators">
  <div slot="calculator">
    <div class="grid md:grid-cols-2 gap-6">
      <div>
        <label class="block text-sm font-semibold text-[#1B4965] mb-1">Area Length (ft)</label>
        <input type="number" id="areaL" value="10" min="0.1" step="0.1" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35] focus:border-transparent" />
      </div>
      <div>
        <label class="block text-sm font-semibold text-[#1B4965] mb-1">Area Width (ft)</label>
        <input type="number" id="areaW" value="8" min="0.1" step="0.1" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35] focus:border-transparent" />
      </div>
      <div>
        <label class="block text-sm font-semibold text-[#1B4965] mb-1">Tile Size</label>
        <select id="tileSize" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35]">
          <option value="1">12" × 12"</option>
          <option value="1.5625">15" × 15"</option>
          <option value="2.25" selected>18" × 18"</option>
          <option value="4">24" × 24"</option>
          <option value="0.6944">10" × 10"</option>
          <option value="0.25">6" × 6" (backsplash)</option>
        </select>
      </div>
      <div>
        <label class="block text-sm font-semibold text-[#1B4965] mb-1">Waste Factor</label>
        <select id="waste" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35]">
          <option value="0.05">5% (simple layout)</option>
          <option value="0.10" selected>10% (standard)</option>
          <option value="0.15">15% (diagonal / complex)</option>
          <option value="0.20">20% (intricate pattern)</option>
        </select>
      </div>
      <div>
        <label class="block text-sm font-semibold text-[#1B4965] mb-1">Price Per Tile ($)</label>
        <input type="number" id="pricePerTile" value="3.50" min="0" step="0.01" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35] focus:border-transparent" />
      </div>
    </div>
    <button onclick="calc()" class="mt-6 bg-[#FF6B35] hover:bg-[#e55a2b] text-white font-semibold px-8 py-3 rounded-lg transition-colors w-full md:w-auto">Calculate Tiles Needed</button>
    <div id="result" class="mt-6 hidden">
      <div class="bg-[#1B4965]/5 rounded-xl p-6">
        <h3 class="font-bold text-[#1B4965] text-lg mb-3">Results</h3>
        <div class="grid sm:grid-cols-3 gap-4" id="res"></div>
      </div>
    </div>
  </div>
  <Fragment slot="content">
    <h2>How to Calculate Tiles</h2>
    <p>Measure the total area in square feet. Divide by the area of one tile (in sq ft). Add a waste factor for cuts and breakage. For diagonal layouts, use 15% waste; for straight layouts, 10% is standard.</p>
    <h2>Common Tile Sizes</h2>
    <ul>
      <li><strong>12" × 12"</strong> = 1 sq ft per tile — the most common floor tile</li>
      <li><strong>18" × 18"</strong> = 2.25 sq ft — popular for large rooms</li>
      <li><strong>24" × 24"</strong> = 4 sq ft — modern, fewer grout lines</li>
      <li><strong>6" × 6"</strong> = 0.25 sq ft — common for backsplashes</li>
    </ul>
  </Fragment>
</ToolLayout>
<script is:inline>
function calc() {
  const area = parseFloat(document.getElementById('areaL').value) * parseFloat(document.getElementById('areaW').value);
  const tileSqFt = parseFloat(document.getElementById('tileSize').value);
  const waste = parseFloat(document.getElementById('waste').value);
  const price = parseFloat(document.getElementById('pricePerTile').value) || 0;
  const tilesNeeded = Math.ceil((area / tileSqFt) * (1 + waste));
  const boxes = Math.ceil(tilesNeeded / 10);
  const totalCost = (tilesNeeded * price).toFixed(2);
  document.getElementById('result').classList.remove('hidden');
  document.getElementById('res').innerHTML = `
    <div class="text-center"><div class="text-3xl font-bold text-[#FF6B35]">${tilesNeeded}</div><div class="text-sm text-gray-500">Tiles Needed</div></div>
    <div class="text-center"><div class="text-3xl font-bold text-[#FF6B35]">${area.toFixed(1)} ft²</div><div class="text-sm text-gray-500">Total Area</div></div>
    <div class="text-center"><div class="text-3xl font-bold text-[#2D936C]">$${totalCost}</div><div class="text-sm text-gray-500">Tile Cost</div></div>
  `;
}
</script>
