---
import ToolLayout from '../../components/ToolLayout.astro';
---
<ToolLayout title="Painting Cost Calculator" description="Estimate interior and exterior painting costs including labor and materials." icon="🎨" category="Cost Estimators" categorySlug="cost-estimators">
  <div slot="calculator">
    <div class="grid md:grid-cols-2 gap-6">
      <div><label class="block text-sm font-semibold text-[#1B4965] mb-1">Project Type</label>
        <select id="type" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35]">
          <option value="interior" selected>Interior</option>
          <option value="exterior">Exterior</option>
        </select>
      </div>
      <div><label class="block text-sm font-semibold text-[#1B4965] mb-1">Paintable Area (sq ft)</label><input type="number" id="area" value="500" min="1" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35] focus:border-transparent" /><p class="text-xs text-gray-400 mt-1">Average room ≈ 350-500 sq ft of wall space</p></div>
      <div><label class="block text-sm font-semibold text-[#1B4965] mb-1">Paint Quality</label>
        <select id="quality" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35]">
          <option value="25">Economy ($25/gal)</option>
          <option value="40" selected>Mid-Range ($40/gal)</option>
          <option value="65">Premium ($65/gal — Benjamin Moore, etc.)</option>
        </select>
      </div>
      <div><label class="block text-sm font-semibold text-[#1B4965] mb-1">Hiring a Painter?</label>
        <select id="labor" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35]">
          <option value="0">No — DIY</option>
          <option value="1" selected>Yes — Professional</option>
        </select>
      </div>
      <div><label class="block text-sm font-semibold text-[#1B4965] mb-1">Coats</label>
        <select id="coats" class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-[#FF6B35]">
          <option value="1">1</option>
          <option value="2" selected>2</option>
          <option value="3">3</option>
        </select>
      </div>
      <div><label class="inline-flex items-center gap-2"><input type="checkbox" id="primer" class="rounded text-[#FF6B35]" /><span class="text-sm font-semibold text-[#1B4965]">Primer coat needed</span></label></div>
    </div>
    <button onclick="calc()" class="mt-6 bg-[#FF6B35] hover:bg-[#e55a2b] text-white font-semibold px-8 py-3 rounded-lg transition-colors w-full md:w-auto">Estimate Painting Cost</button>
    <div id="result" class="mt-6 hidden"><div class="bg-[#1B4965]/5 rounded-xl p-6"><h3 class="font-bold text-[#1B4965] text-lg mb-3">Painting Cost Estimate</h3><div id="res"></div></div></div>
  </div>
  <Fragment slot="content">
    <h2>Professional Painting Costs</h2>
    <p>Professional interior painting costs <strong>$2-5 per sq ft</strong> including labor and materials. Exterior painting runs $3-7 per sq ft due to prep work and equipment needs.</p>
    <h2>DIY vs Hiring a Painter</h2>
    <p>DIY painting saves 60-70% on labor costs. A room that costs $800-1,200 professionally may cost $100-200 in materials for DIY. However, prep work (taping, priming, patching) takes significant time.</p>
  </Fragment>
</ToolLayout>
<script is:inline>
function calc(){
  const type=document.getElementById('type').value;
  const area=parseFloat(document.getElementById('area').value);
  const paintCost=parseFloat(document.getElementById('quality').value);
  const pro=document.getElementById('labor').value==='1';
  const coats=parseInt(document.getElementById('coats').value);
  const primer=document.getElementById('primer').checked;
  const gallons=Math.ceil((area*coats)/350);
  const primerGallons=primer?Math.ceil(area/350):0;
  const materialCost=gallons*paintCost+primerGallons*30;
  const laborRate=type==='interior'?2.5:4;
  const laborCost=pro?area*laborRate:0;
  const supplies=pro?0:50; // tape, brushes, rollers, drop cloths
  const total=Math.round(materialCost+laborCost+supplies);
  const items=[];
  items.push({name:`Paint (${gallons} gal × $${paintCost})`,cost:gallons*paintCost});
  if(primer)items.push({name:`Primer (${primerGallons} gal)`,cost:primerGallons*30});
  if(pro)items.push({name:`Labor ($${laborRate}/sqft)`,cost:laborCost});
  else items.push({name:'Supplies (tape, rollers, etc.)',cost:supplies});
  let html=`<div class="text-center mb-4"><div class="text-4xl font-bold text-[#FF6B35]">$${total.toLocaleString()}</div><div class="text-sm text-gray-500">${pro?'Professional':'DIY'} estimate</div></div><div class="space-y-2">`;
  items.forEach(i=>{html+=`<div class="flex justify-between text-sm"><span>${i.name}</span><span class="font-semibold">$${Math.round(i.cost).toLocaleString()}</span></div>`;});
  html+=`</div>`;document.getElementById('result').classList.remove('hidden');document.getElementById('res').innerHTML=html;
}
</script>
